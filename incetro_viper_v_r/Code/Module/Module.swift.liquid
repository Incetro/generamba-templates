{% include 'header' %}

import TransitionHandler

// MARK: - {{ prefix }}{{ module_info.name }}Module

final {{ privacy }}class {{ prefix }}{{ module_info.name }}Module {

    // MARK: - Module

    {{ privacy }}static func instantiate(withTransitionHandler transitionHandler: TransitionHandler) -> {{ prefix }}{{ module_info.name }}View {
        {{ prefix }}{{ module_info.name }}ModuleAssembly().obtainView(withTransitionHandler: transitionHandler)
    }
}
