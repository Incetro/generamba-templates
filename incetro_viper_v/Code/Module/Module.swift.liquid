{% include 'header' %}

// MARK: - {{ prefix }}{{ module_info.name }}Module

final {{ privacy }}class {{ prefix }}{{ module_info.name }}Module {

    // MARK: - Module

    {{ privacy }}static func instantiate() -> {{ prefix }}{{ module_info.name }}View {
        {{ prefix }}{{ module_info.name }}ModuleAssembly().obtainView()
    }
}
