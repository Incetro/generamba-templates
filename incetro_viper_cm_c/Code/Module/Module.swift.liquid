{% include 'header' %}

import TransitionHandler

// MARK: - {{ prefix }}{{ module_info.name }}Module

final {{ privacy }}class {{ prefix }}{{ module_info.name }}Module: Module {

    // MARK: - Aliases

    {{ privacy }}typealias Input = {{ prefix }}{{ module_info.name }}ModuleInput
    {{ privacy }}typealias View = {{ prefix }}{{ module_info.name }}ViewController

    // MARK: - Initializers

    required {{ privacy }}init() {
    }

    // MARK: - Module

    {{ privacy }}static func instantiate() -> {{ prefix }}{{ module_info.name }}ViewController {
        {{ prefix }}{{ module_info.name }}ModuleAssembly().obtainViewController()
    }
}
