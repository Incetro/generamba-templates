{% include 'header' %}

import VIPER

// MARK: - {{ prefix }}{{ module_info.name }}ViewInput

{{ privacy }}protocol {{ prefix }}{{ module_info.name }}ViewInput: ViewInput {

    /// Setup initial view state
    func setupInitialState()

    /// Stop pull to refresh loader
    func stopPullToRefreshIfNeeded()

    /// Setup infinite scroll
    /// - Parameter isEnabled: true if infinite scroll is enabled
    func setupInfiniteScroll(isEnabled: Bool)
}
