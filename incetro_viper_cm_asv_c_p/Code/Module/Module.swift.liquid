{% include 'header' %}

import TransitionHandler

// MARK: - {{ prefix }}{{ module_info.name }}Module

final {{ privacy }}class {{ prefix }}{{ module_info.name }}Module: AdvancedModule {

    // MARK: - Aliases

    {{ privacy }}typealias Input = {{ prefix }}{{ module_info.name }}ModuleInput
    {{ privacy }}typealias View = {{ prefix }}{{ module_info.name }}ViewController
    {{ privacy }}typealias Data = {{ custom_parameters.plain }}PlainObject

    // MARK: - Module

    {{ privacy }}static func instantiate(withData data: Data) -> {{ prefix }}{{ module_info.name }}ViewController {
        {{ prefix }}{{ module_info.name }}ModuleAssembly().obtainViewController(withData: data)
    }
}
