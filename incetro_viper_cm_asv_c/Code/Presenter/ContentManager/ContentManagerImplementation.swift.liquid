{% include 'header' %}

import AloeStackView

// MARK: - {{ prefix }}{{ module_info.name }}ContentManager

final {{ privacy }}class {{ prefix }}{{ module_info.name }}ContentManagerImplementation: NSObject, {{ prefix }}{{ module_info.name }}ContentManager {

    // MARK: - Properties

    /// Current AloeStackView instance
    private let stackView: AloeStackView

    // MARK: - Initializers

    /// Default initializer
    /// - Parameters:
    ///   - stackView: AloeStackView instance
    {{ privacy }}init(stackView: AloeStackView) {
        self.stackView = stackView
        super.init()
    }

    // MARK: - ContentManager

    {{ privacy }}func updateData(_ viewModel: {{ prefix }}{{ module_info.name }}ViewModelProtocol) {
    }
}
